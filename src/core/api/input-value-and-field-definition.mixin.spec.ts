import { InputValuesAsArgumentsApiMixin } from './input-value-and-field-definition'

describe(InputValuesAsArgumentsApiMixin.name, () => {
  const t = InputValuesAsArgumentsApiMixin.prototype

  // eslint-disable-next-line jest/expect-expect
  test(t.getArgnames.name, () => {
    //
  })

  // eslint-disable-next-line jest/expect-expect
  test(t.getArguments.name, () => {
    //
  })

  // eslint-disable-next-line jest/expect-expect
  test(t.getArgumentsByTypename.name, () => {
    //
  })

  // eslint-disable-next-line jest/expect-expect
  test(t.hasArgument.name, () => {
    //
  })


  // eslint-disable-next-line jest/expect-expect
  test(t.getArgument.name, () => {
    //
  })
})
